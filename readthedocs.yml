# readthedocs.yml — Read the Docs build configuration (v2 schema)

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"

python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py
